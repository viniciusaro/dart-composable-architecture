name: composable_architecture_flutter
description: A collection of widgets that enable using The Composable Architecture with the Flutter framework. Built to be used with the composable_architecture package.
version: 0.0.8
repository: https://github.com/viniciusaro/dart-composable-architecture

environment:
  sdk: ^3.0.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  composable_architecture: 0.1.0

# dependency_overrides:
#   composable_architecture:
#     path: ../tca_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0